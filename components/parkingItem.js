import React from 'react'
import {StyleSheet, View, Text, Image, TouchableOpacity} from 'react-native';

const ParkingItem = ({properties}) => {

    render(
        <View>
            <Text>{properties}</Text>
        </View>
    )
}

const styles = StyleSheet.create({

})

export default ParkingItem;